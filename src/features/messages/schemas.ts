/** The message contract, owned by the shared package. See the note in the conversations schema. */
export {
  MAX_MESSAGE_LENGTH,
  messageListSchema,
  messagePageSchema,
  messageSchema,
  sendMessageInputSchema,
  type Message,
  type MessagePage,
  type MessageStatus,
  type SendMessageInput,
} from '@/lib/contracts'
